/*
	pinnable (selected)

	show payload
	show child pcode / click to jump to text or open a new pcodeview


*/
var Pcodeview=React.createClass({
	render :function() {
		return <div>Pcodeview</div>
	}
});
module.exports=Pcodeview;